---
title: Configuration
weight: 50
description: config/cbox.php — the pinned addon versions and the tunnel image.
---

# Configuration

There is very little to configure, on purpose. The versions this platform runs
are pinned rather than chosen per machine, because the parity claim is only worth
something if everybody meets the same thing.

- **[Reference](reference.md)** — every key, its value, and why it is pinned.
